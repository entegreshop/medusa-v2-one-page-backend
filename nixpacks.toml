[phases.setup]
aptPkgs = []

[phases.install]
cmds = ["npm ci --no-audit --no-fund --prefer-offline"]

[start]
cmd = "cd apps/backend && node wait-for-db.js && npx medusa db:migrate && npx medusa start --port ${PORT:-3000}"

